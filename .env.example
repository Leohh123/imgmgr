# 复制本文件为 .env，并按当前开发环境修改。
# 不要在 .env.example 中填写私有安装路径。

# Qt 安装目录，例如：/path/to/Qt/6.x.x/<compiler>
QT_ROOT=

# MinGW bin 目录，例如：/path/to/Qt/Tools/<mingw>/bin
MINGW_BIN=

# 可选配置
BUILD_DIR=build
GENERATOR="MinGW Makefiles"
JOBS=8
